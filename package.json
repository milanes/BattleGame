{
  "name": "lol",
  "version": "1.0.0",
  "description": "**Versión muy muy muy básica**",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/index.html --open",
    "build": "parcel build src/index.html -d build --public-url BattleGame/",
    "deploy": "git subtree push --prefix build origin gh-pages"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    "eslint": "^5.16.0"
  }
}
